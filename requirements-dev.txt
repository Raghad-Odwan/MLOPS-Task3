# Development-only dependencies: testing, linting, and everything needed
# to work on the project, but not needed to run the service in production.
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
dvc==3.59.0
black==24.10.0
flake8==7.1.1
